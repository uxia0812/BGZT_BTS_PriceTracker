# 📊 Google Analytics 4 설정 가이드

BTS 포토카드 시세 페이지에 Google Analytics 4 (GA4)를 연결하여 상세한 트래픽 분석을 하는 방법입니다.

## 🎯 GA4로 확인할 수 있는 정보

### 무료로 모든 기능 사용 가능:
- 📊 **실시간 방문자** - 지금 몇 명이 보고 있는지
- 👥 **총 방문자 수** - 일별, 주별, 월별 방문자
- ⏱️ **평균 체류 시간** - 사용자가 얼마나 오래 머무는지
- 🖱️ **이벤트 추적** - 클릭, 스크롤, 다운로드 등
- 📍 **지역 정보** - 어느 나라/도시에서 접속하는지
- 📱 **디바이스 정보** - 모바일 vs 데스크톱 비율
- 🌐 **브라우저/OS** - Chrome, Safari, Windows, Mac 등
- 🔗 **유입 경로** - 구글 검색, 직접 방문, SNS 등
- 📈 **페이지별 조회수** - 한국어 vs 영어 페이지 비교
- 🔄 **이탈률** - 페이지를 바로 나가는 비율
- 📊 **사용자 흐름** - 사용자가 어떤 경로로 이동하는지

## 📋 설정 단계

### 1️⃣ Google Analytics 계정 생성

1. [Google Analytics](https://analytics.google.com/) 접속
2. **측정 시작** 클릭
3. 계정 이름 입력 (예: "BTS Photocard Tracker")
4. **다음** 클릭

### 2️⃣ 속성 만들기

1. 속성 이름 입력 (예: "BTS Photocard Price Guide")
2. **보고 시간대**: `대한민국 표준시` 선택
3. **통화**: `대한민국 원 (KRW)` 선택
4. **다음** 클릭

### 3️⃣ 비즈니스 정보 입력

1. 업종 카테고리: `기타` 또는 `커뮤니티 및 콘텐츠`
2. 비즈니스 규모: `소규모` 선택
3. Google 애널리틱스를 사용하려는 이유:
   - ✅ 사용자 행동 분석
   - ✅ 사이트 측정
4. **만들기** 클릭

### 4️⃣ 데이터 스트림 생성

1. 플랫폼 선택: **웹** 클릭
2. 웹사이트 URL 입력:
   - `https://bgzt-bts-price-tracker.vercel.app` (또는 실제 도메인)
3. 스트림 이름: `BTS Photocard Website`
4. **스트림 만들기** 클릭

### 5️⃣ 측정 ID 복사

웹 스트림 상세정보 페이지에서 **측정 ID**를 확인합니다:
```
G-XXXXXXXXXX
```
이 형식의 ID를 복사하세요. (예: `G-ABC123XYZ`)

### 6️⃣ Python 스크립트에 측정 ID 추가

`bts_photocard_analyzer.py` 파일을 열어서 **83번째 줄 근처**를 수정:

**수정 전:**
```python
GA4_MEASUREMENT_ID = os.environ.get('GA4_MEASUREMENT_ID', '')
```

**수정 후:**
```python
GA4_MEASUREMENT_ID = os.environ.get('GA4_MEASUREMENT_ID', 'G-ABC123XYZ')
```
`G-ABC123XYZ` 부분을 5단계에서 복사한 실제 측정 ID로 교체하세요.

### 7️⃣ HTML 재생성

터미널에서 실행:
```bash
python3 bts_photocard_analyzer.py --skip-validate --all-locales
```

### 8️⃣ Git 커밋 & Vercel 배포

```bash
git add .
git commit -m "Add Google Analytics 4"
git push
```

Vercel이 자동으로 배포를 시작합니다!

### 9️⃣ 데이터 수집 확인

1. 배포 완료 후 사이트 방문
2. [Google Analytics](https://analytics.google.com/) 접속
3. 좌측 메뉴: **보고서** → **실시간**
4. 실시간 사용자가 1명 이상 표시되면 성공! 🎉

## 📊 주요 리포트 보는 법

### 실시간 사용자 확인
1. **보고서** → **실시간**
2. 지금 사이트를 보고 있는 사용자 수 확인

### 일별 방문자 확인
1. **보고서** → **생애주기** → **획득** → **트래픽 획득**
2. 날짜 범위 선택 (우측 상단)
3. 일별/주별 방문자 그래프 확인

### 페이지별 조회수
1. **보고서** → **생애주기** → **참여** → **페이지 및 화면**
2. 어떤 페이지가 가장 인기있는지 확인

### 사용자 위치 확인
1. **보고서** → **사용자** → **사용자 속성** → **개요**
2. 국가/도시별 방문자 확인

### 평균 체류 시간
1. **보고서** → **참여** → **개요**
2. "평균 참여 시간" 확인

## 🎯 맞춤 이벤트 추가 (선택사항)

### 피드백 제출 추적

이미 피드백 제출 시 자동으로 이벤트가 기록됩니다!

추가로 특정 행동을 추적하고 싶다면:

```javascript
// 예: 차트 클릭 추적
gtag('event', 'chart_click', {
  'member': 'Jungkook',
  'photocard_type': '럭드포'
});
```

## 🔐 개인정보 보호

GA4는 자동으로 IP 익명화, 쿠키 관리를 합니다.

만약 GDPR 준수가 필요하다면:
```javascript
gtag('consent', 'default', {
  'analytics_storage': 'denied'
});
```

## 📱 모바일 앱 추적 (향후 확장 시)

GA4는 웹 + 앱을 통합 추적할 수 있습니다.
나중에 모바일 앱을 만들면 같은 GA4 속성에 데이터를 통합할 수 있습니다.

## ⚠️ 문제 해결

### "데이터가 수집되지 않습니다" 표시
- 24시간 이내에 자동으로 해결됩니다
- 실시간 보고서에서는 즉시 확인 가능합니다

### 로컬에서 테스트 시 데이터가 수집됨
- `file:///` 프로토콜에서는 GA4가 작동하지 않습니다
- Vercel 배포 후에만 정상 작동합니다

### 내 방문도 카운트됨
- GA4 설정 → **데이터 스트림** → **고급 설정**
- **내부 트래픽 정의** 추가
- 본인 IP 주소 입력

## 📊 대시보드 예제

### 주간 리포트 보기
1. **탐색** → **빈 보고서**
2. 측정기준 추가: `날짜`, `국가`, `페이지 제목`
3. 측정항목 추가: `사용자`, `세션`, `평균 참여 시간`
4. 즐겨찾기에 저장

### 실시간 모니터링
- **보고서** → **실시간**
- 지금 이 순간 몇 명이 보고 있는지
- 어느 페이지를 보는지
- 어느 국가에서 접속했는지

## 🎉 완료!

이제 다음 정보를 모두 확인할 수 있습니다:
- ✅ 실시간 방문자
- ✅ 총 방문자 수
- ✅ 평균 체류 시간
- ✅ 클릭 및 이벤트
- ✅ 지역/디바이스 정보
- ✅ 유입 경로

**완전 무료**, **무제한 데이터**, **평생 사용 가능**합니다! 🎊

---

궁금한 점이 있으면 [Google Analytics 고객센터](https://support.google.com/analytics)를 참고하세요!
